# Рабочая установка: 2026-09-21

Развёрнут комплект `local-system-prompt-v1` на `/home/ae/services/openhands`.
Все три сервиса используют `local/openhands-canvas:1.20.0-sdk1.49.1-system-prompt.1`,
image ID `sha256:79c650eea206223ef17d0716e9b9face7f9df25f16d98b4b1d3eb1f0a7c01074`.

Перед заменой сервисы остановлены, согласованная резервная копия проверена:
`/home/ae/services/openhands/backups/before-system-prompt-20260921-231456/installation.tar.gz`.
Копия локальная, содержит секреты, в Git не входит.

Проверено после запуска:
- server, canvas, automation: healthy; readiness/health endpoints HTTP 200;
- Canvas через tailnet: HTTP 200, новые строки редактора доступны;
- тестовый Agent Profile сохранил промпт дословно, новый разговор сформировал
  соответствующий SystemPromptEvent; LLM не запускалась;
- тестовый профиль и разговор удалены; сохранилось 9 прежних разговоров;
- существующие агентские и LLM-профили, secrets.env не изменились.

Промпты существующих профилей автоматически не перенастраивались.
Для дальнейших операций Compose использовать оба файла, из каталога сервиса:

```sh
docker compose -f compose.yaml -f customization/compose.custom.yaml up -d
```

Обычный `docker compose up -d` без overlay вернёт upstream image; порядок
безопасного отката описан в README. Push новых коммитов сам по себе ничего
не развёртывает. Данная запись фиксирует результат проверки на указанную дату.

## 2026-09-22: возврат эксплуатационных параметров к дефолтам

По запросу владельца восстановлены `num_retries=5` во всех трёх LLM-профилях
и глобальных настройках, `max_iterations=500` в глобальных настройках.
В последнем разговоре `baebec21-805d-4508-8607-52d55d4978d9` также выставлены
5 повторов и 500 итераций; предыдущие разговоры не переписывались.
Согласованный `max_output_tokens=32768` сохранён в профилях, общих настройках
и последнем разговоре.

Из локального compose.yaml удалены OH_MAX_CONCURRENT_RUNS=1 (дефолт сервера 10),
mem_limit и pids_limit всех трёх контейнеров. Из глобальной конфигурации MCP
удалены timeout/sse_read_timeout=120: используются штатные значения MCP-клиента.
Condenser и verification проверены по моделям установленного SDK и совпадают
с их дефолтами. Модели, адреса, ключи, MCP, промпты, режим chat локальных
провайдеров и реальные размеры их контекста сохранены. Настройки размещения,
доступа и отключённой телеметрии сохранены.

Перед изменениями создана локальная резервная копия (содержит секреты):
`/home/ae/services/openhands/backups/normalize-defaults-20260922-001356`.
Контейнеры пересозданы с обоими Compose-файлами и прежним custom image.
Генерация ответов и автоматическое продолжение разговоров не запускались.
Проверка после запуска: API подтвердил настройки профилей, общие настройки
и настройки последнего разговора; Server /ready, Canvas /canvas и Automation
health ответили HTTP 200. Docker inspect подтвердил Memory=0, PidsLimit=null
и отсутствие OH_MAX_CONCURRENT_RUNS в окружении всех контейнеров.
